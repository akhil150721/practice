--- 
 - hosts: ubuntuserver
   become: yes
   tasks: 
     - name: install tree on ubuntu node
       apt:
         name: "{{first}}"
         state: present
       when: ansible_os_family== "Debian"      

     - name: instal webserver
       apt: 
        name: "{{second}}"
        state: present 
       when: ansible_os_family== "Debian"     